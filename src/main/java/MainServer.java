import threadpools.SharedThreadPools;import java.io.IOException;/** * Created by demopoo on 2017/9/21. */public class MainServer {    private final int port;    private final String ipAddress;    public MainServer(String ipAddress,int port){        this.ipAddress = ipAddress;        this.port = port;    }    public void main() throws IOException{        //根据CPU核心数创建多个reactor 一般是 1 ：1        int cpuCount = Runtime.getRuntime().availableProcessors();        Elena_Reactor[] elena_reactors = new Elena_Reactor[cpuCount];        for (int i = 0;i < cpuCount;i++){            elena_reactors[i] = new Elena_Reactor(SharedThreadPools.getExcutor());        }    }}