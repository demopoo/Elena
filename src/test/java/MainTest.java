import bufferpools.DirectByteBufferPools;import sun.nio.ch.DirectBuffer;import java.nio.ByteBuffer;import java.util.BitSet;import java.util.concurrent.ThreadLocalRandom;/** * Created by demopoo on 2017/9/22. */public class MainTest {    public static void main(String[] args) {        DirectByteBufferPools byteBufferPools = new DirectByteBufferPools(10,10485760,4096);        ByteBuffer byteBuffer = byteBufferPools.allocateBuffer(4097);//2个        System.out.println(1);        ByteBuffer byteBuffer1 = byteBufferPools.allocateBuffer(8193);//3个        System.out.println(2);        byteBufferPools.recycleBuffer(byteBuffer1);        System.out.println(3);        byteBufferPools.recycleBuffer(byteBuffer);        System.out.println(4);//        ByteBuffer byteBuffer = ByteBuffer.allocateDirect(1024);//        byteBuffer.position(150);//        byteBuffer.limit(400);//        ByteBuffer bf = byteBuffer.slice();////        DirectBuffer directBuffer = (DirectBuffer)byteBuffer;//        DirectBuffer db = (DirectBuffer) bf;////        DirectBuffer buffer = (DirectBuffer) db.attachment();////        System.out.println(directBuffer.address());//        System.out.println(db.address());//        System.out.println(db.address() - directBuffer.address());//        BitSet bitSet = new BitSet();////        bitSet.set(10);////        System.out.println(bitSet.size());//        System.out.println(bitSet.get(10));//        System.out.println(bitSet.get(11));//        bitSet.set(65);//        System.out.println(bitSet.size());//        for (int i=0;i<10;i++){//            System.out.println(ThreadLocalRandom.current().nextInt(10));//        }//        int cpuCount = Runtime.getRuntime().availableProcessors();//        System.out.println(cpuCount);    }}